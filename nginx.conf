server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # SPA at site root so https://dira365.com/login works.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
