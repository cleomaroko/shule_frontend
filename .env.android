# Used only by `npm run build:android`. Vite inlines this into the APK bundle.
# Web/Docker builds keep VITE_API_BASE_URL=/api (see Dockerfile).
VITE_API_BASE_URL=https://dira365.com/api
VITE_APP_NAME=Dira 365
