# IP vhost (162.35.96.90): keep backend, crash, aviator as they are.
# After the SPA is built at `/`, old /dira/ bookmarks should go to the domain.
#
# Replace the existing "SHULE FRONTEND (Dira)" location /dira/ block with:

    location /dira/ {
        rewrite ^/dira/?(.*)$ http://dira365.com/$1 redirect;
    }
